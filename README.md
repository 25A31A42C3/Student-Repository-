# Student-Repository-
This project is a simple file-based repository system implemented in the C programming language. It demonstrates how to manage structured data (student records) using binary file storage, simulating basic database operations.  The system allows users to store and retrieve student information such as ID, name, and marks.
